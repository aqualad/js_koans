global.expect = require("chai").expect;
describe("The beginning of your ascent", () => {
  it('will fail', () => {
    expect(false).to.be.true;
  })

  it('will expand your mind', ()=> {
    expect(false).to.be.false;
  })
})